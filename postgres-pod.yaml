apiVersion: v1
kind: Pod
metadata:
 name: postgres-pod
 labels:
  name: postgres-pod
  team: rus

spec:
 containers:
 - name: postgres-container
   image: postgres:9.4
   ports:
   - containerPort: 5432
