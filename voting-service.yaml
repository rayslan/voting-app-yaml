apiVersion: v1
kind: Service
metadata:
 name: voting-service
 label:
  name: voting-service
  team: rus

spec:
 type: LoadBalancer
 ports:
 - port: 80
   targetPort: 80
 selector:
  matchLabels:
   name: voting_app-pod
   team: rus
